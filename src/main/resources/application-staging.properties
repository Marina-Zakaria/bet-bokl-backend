# Staging is activated together with prod: SPRING_PROFILES_ACTIVE=prod,staging
# It inherits production DB, logging, error-handling, CORS, and Liquibase rules.

# Temporary staging-only OTP requested for frontend integration testing.
otp.fixed-value=111111
